export class Method {
    constructor(flags, name, descriptor, attributes) {
        this.flags = flags
        this.name = name
        this.descriptor = descriptor
        this.attributes = attributes
    }
}
